# Baseline scientific environment copied from the current CatFORGE core.
# These pins are not yet an archival, platform-specific lockfile. Validate the
# exact paper environment and record Python/OS/package hashes before release.

pandas==2.3.3
numpy==2.5.2
scikit-learn==1.9.0
xgboost==3.4.1
joblib==1.6.0

sdv==1.38.2
sdmetrics==0.30.0

matplotlib==3.11.1
plotly==7.0.0
kaleido==1.3.0
umap-learn>=0.5,<0.6

# `data_synthesizer.py` imports torch at module import time.
torch==2.13.0

shap==0.52.0
lime==0.2.0.1

pytest==9.1.1
