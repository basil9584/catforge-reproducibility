# Expected paper outputs

After the permitted study data and final configurations are available, commit lightweight, reviewable expected outputs here: metric JSON files, figure-source CSV files and SHA-256 manifests. Do not commit model pickle files or opaque runtime folders. A deterministic figure-generation script should read these tracked inputs and recreate every manuscript table and figure.
